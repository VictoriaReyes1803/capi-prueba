// Copy this file to environment.ts and environment.production.ts
// and fill in the real values. Those files are gitignored.
export const environment = {
  production: false,
  apiUrl: 'http://YOUR_HOST:YOUR_PORT/api',
};
